server:
  port: 8081

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/authapp
    username: root
    password: Himanshu123@

  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
        format_sql: true
    show-sql: true
